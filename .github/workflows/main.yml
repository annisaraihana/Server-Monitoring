on: 
  push:
    branches:
      - main
name: 🚀 Deploy website on push main
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@3.1.2
      with:
        ftp-server: "sftp://10.100.254.116:22/var/www/html/monitoring"
        ftp-username: ${{ secrets.FTP_USERNAME }}
        ftp-password: ${{ secrets.FTP_PASSWORD }}
        git-ftp-args: --dry-run --insecure
